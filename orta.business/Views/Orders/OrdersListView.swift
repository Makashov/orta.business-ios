import SwiftUI
import Playgrounds

struct OrdersListView: View {
    var body: some View {
        Text("Hello, world!")
            .padding()
            .appBackground()
            .mainHeader()
    }
}

#Preview {
    OrdersListView()
}
